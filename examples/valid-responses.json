{
  "tools": [
    {
      "type": "function",
      "name": "get_weather",
      "parameters": {
        "type": "object",
        "properties": {
          "city": {"type": "string"},
          "units": {"type": "string", "enum": ["celsius", "fahrenheit"]}
        },
        "required": ["city", "units"],
        "additionalProperties": false
      }
    }
  ],
  "response": {
    "id": "resp_fixture_1",
    "output": [
      {
        "type": "function_call",
        "id": "fc_1",
        "call_id": "call_weather_1",
        "name": "get_weather",
        "arguments": "{\"city\":\"Melbourne\",\"units\":\"celsius\"}"
      }
    ]
  }
}
